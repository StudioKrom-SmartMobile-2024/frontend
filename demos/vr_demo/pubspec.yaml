name: vr_demo
description: "A Demo project"
publish_to: "none"
version: 0.1.1

environment:
  sdk: ">=3.3.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_soloud: ^2.0.2
  vr_player: ^0.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/cardboard.png
